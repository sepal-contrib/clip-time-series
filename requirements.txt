# these libs are requested to build application env
jupyter
voila
toml

# the base lib to run any sepal_ui based app
# don't forget to fix it to a specific version when you're app is ready
sepal-ui

# custom libs
ipyvuetify==1.8.5
earthengine-api
geopandas
natsort
pypdf
Wand==0.6.7
planet==2.0a6 # same as sepal_ui
unidecode
